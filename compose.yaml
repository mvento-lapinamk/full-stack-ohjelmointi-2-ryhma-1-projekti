services:
  backend:
    build: ./Backend
    ports: ["3000:3000"]
    env_file: ./Backend/.env
    restart: unless-stopped

  frontend:
    build: ./Frontend
    ports: ["5173:80"]
    depends_on: [backend]
    restart: unless-stopped